{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "(lldb-mac) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/dist/LogViewer.app",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build with CMake"
        },
    ]
}