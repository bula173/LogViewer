@PACKAGE_INIT@

# Basic package config that exposes the plugin API targets installed to sdk/lib/cmake
include("${CMAKE_CURRENT_LIST_DIR}/LogViewerPluginTargets.cmake")

message(STATUS "LogViewer CMake package loaded (plugin API only)")
